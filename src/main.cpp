#include "cgalabc.hpp"

int main(){
  cg::Point2d mypoint(1,2);

  mypoint.print();

  /* Potential use case
   * vec dot vec
   * vec cross vec
   * shortest distance of point to line
   * shortest distance of point to vector
   *
   */
  return 0;
}
